Final what done in Project.

1. import library and datasets. Train data as fintrain and test data as finpreidct.
2. Split the fintrain dataset (into 80:20 ratio).
3. Create a source column to identify the origin of data.
4. Combine all 3 dataset.

----------------------EDA Works----------------------
5. Finding columns, describe, shape, info, null values.
6. Drop REF_NO and Post Code because it a identity columns.
7. Create count plot to find the unknown values in categorical columns.
8. Converting the features of 'Zero' to 0 & +4 to 4 and convert dtype from object to int (because it is a numerical column - 0, 1, 2, 3).
9. Replace unknown values with their respective modes of gender columns.
10. Create count plot for numerical columns to find the unique values.
11. Finding the unqiue value of Revenue Grid which is our tagert column.
12. Create pie chart to see the distribution of revenue grid (1.0 class - 10.62% & 2.0 class -89.38%) to see the data was balance or not.  
13. Finding the unqiue value of age band.
14. Replace 45-50 to 46-50, 65-70 to 66-70 & 55-60 to 56-60 because it overlapping due to 41-45, 61-65 & 51-55.
15. Replacing unknown value with thier respective modes in age band columns.
16. Replacing unkown value with thier respective modes in family income.
17. Finding unique values of Post area, Tv area & region.
18. Drop post area because it contains 2039 not easy to encode.

--------------------------------Outliers----------------------
19. Finding Outliers by IQR method and create a boxplot to show the outliers.
20. Remove Outliers by capping method and create boxplot to show outliers remover.
21. Find correlation between numerical columns and create heatmap to show the correlation.

--------------------------Encoding--------------------------
22. Encoding - ordinal encoding on age band & family income to maintains the order of categorical, useful for features with a natural ranking. 
23. Binary encoding on gender to reduced dimensionality compared to one hot encoding, helping to minimize multicollinearity.
24. One hot encoding on staute, occupation, occupation partner, home_status, self_employed, self_employed_partner, TVarea, region, Source.

--------------------------Separate all 3 datasets------------------------
25. Separate all 3 datasets and find the shape of traindf, testdf & finpredict.

------------------------ Feature Selection--------------------------
26. Drop NaN value and find the top 20 features by ANOVA F-Test and RFE(Recursive feature Elimination).

------------------------ Correlation Metrix-------------------------
27. Finding the correlation Metrix and create heatmap.

------------------------ Split the encoded dataset--------------------------
28. Split the fintrain encoded data and separate the independent and dependent variable.

-------------------------Scaling------------------------
29. Idenify the scale of dataset.
30. Scaling on fintrain encoded- Standardization

------------------------Model Development-------------------------
31. Model development - Logistic regression, Decision Tree, Random Forest, Support Vector & K-Neatest Neighbour. And Evaluation Metrix.


-------------------------Model Development with RFE & ANOVA------------------------
32. Combine ANOVA & RFE top features.
33. Split the avona data (which combination of ANOVA & RFE top features) and separate the independent and dependent variable.
34. Standardization on selected features.
35. Develop models - Logistic regression, Decision Tree, Random Forest, Support Vector & K-Neatest Neighbour. And Evaluation Metrix.

-------------------------Visualization of evaluation matrix----------------
36. Create bar plot to compare class1 & class2 the evaluation matrix of all models (all features and top features models).

-------------------------Check the overfitting in model---------------------
37. In random forest with all features by accuracy, f1 score & confusion matrix of train and test dataset.

-------------------------SMOTE technique--------------------
38. Apply SMOTE (on all features).
39. Scaling - Standardization.
40. Develop model - Support Vector Machine, Random Forest, K-Nearest Neighbour & Logistic Regression. And Evaluation Metrix.


Final model was Random Forest (with all features).








